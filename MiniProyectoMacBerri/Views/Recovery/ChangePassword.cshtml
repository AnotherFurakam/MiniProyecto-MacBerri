@{
    ViewData["Title"] = "ChangePassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model MiniProyectoMacBerri.ModelsViews.RecoveryViewModel

<div class="d-flex w-100 h-100 justify-content-center align-items-center">
    <form class="shadow p-5 rounded-3 change-password" asp-controller="Recovery" asp-action="ChangePassword" method="post">
        <h1 class="display-5 fw-normal mt-3 mb-5">Cambia tu contraseña</h1>
        <div>
            <h2>@Html.DisplayFor(m => @Model.User.Names)</h2>
        </div>
        <div>
            <label class="form-label">Contraseña:</label>
            <input class="form-control" type="password" asp-for=@Model.RecoveryPasswordDto.Password />
        </div>
        <div class="my-3">
            <button class="btn btn-primary w-100">Cambiar</button>
        </div>
        <div class="border border-secondary rounded-2 p-2">
            <h5 class="text-dark">Nota:</h5>
            <p class="text-secondary">
                Lo ideal sería enviar un correo con un link cuyo parametro de ruta sería un código aleatorio o un jwt que al validarse permitirá el cambio de contraseña.
            </p>
        </div>
    </form>
</div>

